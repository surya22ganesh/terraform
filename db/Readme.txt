sudo yum update -y

sudo yum install mysql -y

Then Connect to mysql using :

type 1:

mysql -u admin -padmin123 -h ee-instance-demo.c9auqscq8pp7.us-east-2.rds.amazonaws.com

type 2:

mysql -u USERNAME -pPASSWORD -h HOSTNAMEORIP DATABASENAME

type 3:

mysql -h <Endpoint> -u username -p -> then press Enter

